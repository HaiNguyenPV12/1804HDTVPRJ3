
@{
    ViewBag.Title = "Index Admin";
    var errors = AirlinesReservationSystem.Models.arsadmin.InsertDAO.error;
}

<h2>Index Admin</h2>

<a class="btn btn-ars-dark" href="/arsadmin/adddb">insert route + flight</a>

<ul>
    @foreach (var item in errors)
    {
        <li>@item.Error</li>
    }
</ul>